# Domain rules

This directory is the single source of truth for character rules and derived values.

UI code must not reimplement D&D calculations. UI reads domain results and dispatches character-state changes.

Content from PHB 2014 is core. Xanathar and Tasha content is source-tagged and campaign-configurable. Homebrew is explicit and never silently changes core rules.
