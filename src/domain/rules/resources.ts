import type {Character,Resource} from '../types';
export function classResources(c:Character):Resource[]{const out:Resource[]=[];for(const cl of c.classes){const push=(id:string,name:string,max:number,recharge:'short'|'long',unlimited=false)=>out.push({id:`${cl.id}:${id}`,name,current:max,max,recharge,unlimited});switch(cl.id){case'barbarian':if(cl.level>=1)push('rage','Rage',cl.level>=20?0:cl.level>=17?6:cl.level>=12?5:cl.level>=6?4:cl.level>=3?3:2,'long',cl.level>=20);break;case'bard':if(cl.level>=1)push('bardic-inspiration','Bardic Inspiration',Math.max(1,Math.floor((c.abilityScores.cha-10)/2)),cl.level>=5?'short':'long');break;case'cleric':if(cl.level>=2)push('channel-divinity','Channel Divinity',cl.level>=18?3:cl.level>=6?2:1,'short');break;case'monk':if(cl.level>=2)push('ki','Ki',cl.level,'short');break;case'paladin':if(cl.level>=1)push('lay-on-hands','Lay on Hands',cl.level*5,'long');if(cl.level>=3)push('channel-divinity','Channel Divinity',1,'short');break;case'sorcerer':if(cl.level>=2)push('sorcery-points','Sorcery Points',cl.level,'long');break;case'fighter':if(cl.level>=1)push('second-wind','Second Wind',cl.level>=17?2:1,'short');if(cl.level>=2)push('action-surge','Action Surge',cl.level>=17?2:1,'short');break;default:break}}return out}
