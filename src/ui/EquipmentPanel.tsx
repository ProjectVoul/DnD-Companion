import {useState} from 'react';
import type {Character,DamageType,Die,Item,ItemKind} from '../domain/types';
const kinds:ItemKind[]=['armor','weapon','shield','focus','gear','magic'];
const damageTypes:DamageType[]=['slashing','piercing','bludgeoning','fire','cold','acid','lightning','necrotic','radiant','poison','psychic','force','thunder'];
const dice:Die[]=[4,6,8,10,12,20];
export function EquipmentPanel({c,update}:{c:Character;update:(fn:(x:Character)=>Character)=>void}){const [kind,setKind]=useState<ItemKind>('weapon');const [editing,setEditing]=useState<string|null>(null);const add=(k:ItemKind)=>{const item:Item={id:crypto.randomUUID(),name:k==='weapon'?'New Weapon':k==='armor'?'New Armor':k==='shield'?'New Shield':'New Item',kind:k,quantity:1,equipped:false,description:'',homebrew:k==='magic'?false:undefined,...(k==='weapon'?{weapon:{attackAbility:'str',proficient:false,attackBonus:0,damageBonus:0,damage:[{dice:1,die:8,type:'slashing'}],properties:[]}}:{}),...(k==='armor'?{armor:{category:'light',baseAC:11,dexBonus:true,stealthDisadvantage:false,magicBonus:0}}:{}),...(k==='shield'?{shield:{acBonus:2,magicBonus:0}}:{})};update(x=>({...x,items:[...x.items,item]}));setEditing(item.id)};return <div className="grid"><section className="card"><div className="row"><div><h2>Equipment</h2><p className="muted">Miscellaneous and Equipment are kept separate; equipment is grouped by type.</p></div><select value={kind} onChange={e=>setKind(e.target.value as ItemKind)}>{kinds.map(k=><option key={k}>{k}</option>)}</select><button onClick={()=>add(kind)}>+ Add item</button></div>{kinds.map(k=><div key={k}><h3>{k[0].toUpperCase()+k.slice(1)}</h3>{c.items.filter(i=>i.kind===k).map(i=><div className="item" key={i.id}><button className="item-main" onClick={()=>setEditing(i.id)}><b>{i.name}</b><small>{i.description||'No description'}{i.homebrew?' · Homebrew':''}</small></button><span>{i.equipped?'Equipped':'Carried'}</span></div>)}</div>)}</section>{editing&&<ItemEditor item={c.items.find(i=>i.id===editing)!} update={update} close={()=>setEditing(null)}/>}</div>}
function ItemEditor({item,update,close}:{item:Item;update:(fn:(x:Character)=>Character)=>void;close:()=>void}){const patch=(p:Partial<Item>)=>update(x=>({...x,items:x.items.map(i=>i.id===item.id?{...i,...p}:i)}));const wp=(p:Partial<NonNullable<Item['weapon']>>)=>patch({weapon:{...item.weapon!,...p}});const ap=(p:Partial<NonNullable<Item['armor']>>)=>patch({armor:{...item.armor!,...p}});const sp=(p:Partial<NonNullable<Item['shield']>>)=>patch({shield:{...item.shield!,...p}});const effects=item.effects??[];return <section className="card"><div className="row"><h2>Edit {item.kind}</h2><button onClick={close}>Done</button></div><label>Name<input value={item.name} onChange={e=>patch({name:e.target.value})}/></label><label>Quantity<input type="number" min="1" value={item.quantity} onChange={e=>patch({quantity:Math.max(1,Number(e.target.value))})}/></label><label>Weight<input type="number" min="0" value={item.weight??''} onChange={e=>patch({weight:e.target.value?Number(e.target.value):undefined})}/></label><label>Description<textarea value={item.description??''} onChange={e=>patch({description:e.target.value})}/></label><label>Effects<textarea value={effects.join('\n')} placeholder="One mechanical or descriptive effect per line" onChange={e=>patch({effects:e.target.value.split('\n').map(x=>x.trim()).filter(Boolean)})}/></label><label><input type="checkbox" checked={!!item.homebrew} onChange={e=>patch({homebrew:e.target.checked})}/> Homebrew item</label><label><input type="checkbox" checked={item.equipped} onChange={e=>patch({equipped:e.target.checked})}/> Equipped</label>{item.weapon&&<><h3>Weapon</h3><label>Attack ability<select value={item.weapon.attackAbility} onChange={e=>wp({attackAbility:e.target.value as 'str'|'dex'|'con'|'int'|'wis'|'cha'})}>{['str','dex','con','int','wis','cha'].map(a=><option key={a}>{a}</option>)}</select></label><label>Attack bonus<input type="number" value={item.weapon.attackBonus} onChange={e=>wp({attackBonus:Number(e.target.value)})}/></label><label>Damage bonus<input type="number" value={item.weapon.damageBonus} onChange={e=>wp({damageBonus:Number(e.target.value)})}/></label><label><input type="checkbox" checked={item.weapon.proficient} onChange={e=>wp({proficient:e.target.checked})}/> Proficient</label><h3>Damage components</h3>{item.weapon.damage.map((d,i)=><div className="damage-row" key={i}><input type="number" min="1" value={d.dice} onChange={e=>{const damage=[...item.weapon!.damage];damage[i]={...d,dice:Math.max(1,Number(e.target.value))};wp({damage})}}/><select value={d.die} onChange={e=>{const damage=[...item.weapon!.damage];damage[i]={...d,die:Number(e.target.value) as Die};wp({damage})}}>{dice.map(x=><option key={x} value={x}>d{x}</option>)}</select><select value={d.type} onChange={e=>{const damage=[...item.weapon!.damage];damage[i]={...d,type:e.target.value as DamageType};wp({damage})}}>{damageTypes.map(x=><option key={x}>{x}</option>)}</select><input type="number" placeholder="Flat" value={d.flat??''} onChange={e=>{const damage=[...item.weapon!.damage];damage[i]={...d,flat:e.target.value?Number(e.target.value):undefined};wp({damage})}}/><button onClick={()=>wp({damage:item.weapon!.damage.filter((_,j)=>j!==i)})}>×</button></div>)}<button onClick={()=>wp({damage:[...item.weapon!.damage,{dice:1,die:6,type:'fire'}]})}>+ Damage component</button></>}{item.armor&&<><h3>Armor</h3><label>Base AC<input type="number" value={item.armor.baseAC} onChange={e=>ap({baseAC:Number(e.target.value)})}/></label><label>Category<select value={item.armor.category} onChange={e=>ap({category:e.target.value as 'light'|'medium'|'heavy'})}><option>light</option><option>medium</option><option>heavy</option></select></label><label><input type="checkbox" checked={item.armor.dexBonus} onChange={e=>ap({dexBonus:e.target.checked})}/> Apply Dexterity</label><label>Dexterity cap<input type="number" value={item.armor.dexCap??''} onChange={e=>ap({dexCap:e.target.value?Number(e.target.value):undefined})}/></label><label>Strength requirement<input type="number" value={item.armor.strengthRequirement??''} onChange={e=>ap({strengthRequirement:e.target.value?Number(e.target.value):undefined})}/></label><label>Magic bonus<input type="number" value={item.armor.magicBonus} onChange={e=>ap({magicBonus:Number(e.target.value)})}/></label><label><input type="checkbox" checked={item.armor.stealthDisadvantage} onChange={e=>ap({stealthDisadvantage:e.target.checked})}/> Stealth disadvantage</label></>}{item.shield&&<><h3>Shield</h3><label>AC bonus<input type="number" value={item.shield.acBonus} onChange={e=>sp({acBonus:Number(e.target.value)})}/></label><label>Magic bonus<input type="number" value={item.shield.magicBonus} onChange={e=>sp({magicBonus:Number(e.target.value)})}/></label></>}</section>}
