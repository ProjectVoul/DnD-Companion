import type {Character,Skill} from '../domain/types';
import {SKILLS,skillBonus} from '../domain/rules';
export function SkillsPanel({c,update}:{c:Character;update:(fn:(x:Character)=>Character)=>void}){return <section className="card"><h2>Skills</h2><p className="muted">P = proficiency · E = expertise. Expertise requires proficiency.</p><div className="skill-list">{(Object.keys(SKILLS) as Skill[]).map(s=>{const st=c.skillStates[s];return <div className="skill" key={s}><span>{SKILLS[s].name}<small>{SKILLS[s].ability.toUpperCase()}</small></span><b>{skillBonus(c,s)>=0?`+${skillBonus(c,s)}`:skillBonus(c,s)}</b><button className={st.proficient?'tick on':'tick'} aria-label={`Toggle proficiency for ${SKILLS[s].name}`} onClick={()=>update(x=>({...x,skillStates:{...x.skillStates,[s]:{proficient:!st.proficient,expertise:st.proficient?false:st.expertise}}}))}>P</button><button className={st.expertise?'tick expert':'tick'} disabled={!st.proficient} aria-label={`Toggle expertise for ${SKILLS[s].name}`} onClick={()=>update(x=>({...x,skillStates:{...x.skillStates,[s]:{proficient:true,expertise:!st.expertise}}}))}>E</button></div>})}</div></section>}
