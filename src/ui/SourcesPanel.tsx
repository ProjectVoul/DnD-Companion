import {CONTENT_SOURCES} from '../domain/content/sources';
import type {Character} from '../domain/types';
export function SourcesPanel({c,update}:{c:Character;update:(fn:(x:Character)=>Character)=>void}){const enabled=new Set(c.contentSources??['phb2014','homebrew']);const toggle=(id:string)=>update(x=>{if(id==='phb2014'||id==='homebrew')return x;const next=new Set(x.contentSources??['phb2014','homebrew']);next.has(id)?next.delete(id):next.add(id);return {...x,contentSources:[...next]}});return <section className="card"><h2>Content Sources</h2><p className="muted">Choose which optional 5e 2014 content is available to this character/campaign. Enabling a source does not silently add every optional feature.</p><div className="choice-grid">{CONTENT_SOURCES.map(s=><button key={s.id} disabled={s.required} className={enabled.has(s.id)?'choice selected':'choice'} onClick={()=>toggle(s.id)}>{s.name}<small>{s.required?'Always enabled':'Optional content pool'}</small></button>)}</div></section>}
