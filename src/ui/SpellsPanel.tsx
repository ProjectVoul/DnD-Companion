import {useMemo,useState} from 'react';
import type {Character,Spell} from '../domain/types';
import {availablePrepared,setPrepared} from '../domain/rules/spellcasting';
import {maxPreparedSpells,spellAttackBonus,spellSaveDC} from '../domain/rules';
export function SpellsPanel({c,update,spells}:{c:Character;update:(fn:(x:Character)=>Character)=>void;spells:Spell[]}){const classId=c.classes[0]?.id??'';const sc=c.spellcasting[classId]??{known:[],prepared:[],alwaysPrepared:[],spellbook:[],slots:{}};const [tab,setTab]=useState<'prepared'|'list'>('prepared');const prepared=availablePrepared(sc);const limit=maxPreparedSpells(c,classId);const grouped=useMemo(()=>spells.reduce<Record<number,Spell[]>>((g,s)=>{(g[s.level]??=[]).push(s);return g},{}),[spells]);const toggle=(id:string)=>update(x=>{const state=x.spellcasting[classId]??{known:[],prepared:[],alwaysPrepared:[],spellbook:[],slots:{}};return {...x,spellcasting:{...x.spellcasting,[classId]:setPrepared(state,id,!state.prepared.includes(id),maxPreparedSpells(x,classId))}}});const spell=(id:string)=>spells.find(s=>s.id===id);return <section className="card"><div className="row"><div><h2>Spells</h2><p className="muted">{c.classes[0]?.name??'Class'} · Spell save DC {spellSaveDC(c,classId)??'—'} · Spell attack {spellAttackBonus(c,classId)===null?'—':`+${spellAttackBonus(c,classId)}`}</p></div><div className="tabs"><button className={tab==='prepared'?'active':''} onClick={()=>setTab('prepared')}>Prepared</button><button className={tab==='list'?'active':''} onClick={()=>setTab('list')}>Class Spell List</button></div></div>{tab==='prepared'?<><h3>Always Prepared</h3>{sc.alwaysPrepared.map(id=><SpellRow key={id} spell={spell(id)} locked/>)}<h3>Prepared by You <small>{sc.prepared.length}/{limit}</small></h3>{sc.prepared.map(id=><SpellRow key={id} spell={spell(id)} active onClick={()=>toggle(id)}/>)}</>:Object.keys(grouped).sort((a,b)=>Number(a)-Number(b)).map(level=><div key={level}><h3>{level==='0'?'Cantrips':`Level ${level}`}</h3>{grouped[Number(level)].map(s=><SpellRow key={s.id} spell={s} active={prepared.includes(s.id)} onClick={()=>toggle(s.id)}/>)}</div>)}</section>}
function SpellRow({spell,onClick,active,locked}:{spell?:Spell;onClick?:()=>void;active?:boolean;locked?:boolean}){if(!spell)return <p className="muted">Spell data unavailable.</p>;return <button className={`spell ${active?'selected':''}`} disabled={locked} onClick={onClick}><span><b>{spell.name}</b><small>{spell.school} · Level {spell.level}{spell.ritual?' · Ritual':''}{spell.concentration?' · Concentration':''}</small></span><i>{locked?'Always prepared':active?'Prepared':'Add'}</i></button>}
